name: "Continuous Integration"

on:
  push:
    branches:
      - main
      - "parse**"
      - "feature/**"
  pull_request:

env:
  MONGODB_SERVER: mongodb://127.0.0.1:27017

jobs:
  phpunit:
    name: "PHPUnit ${{ matrix.php-version }} (${{ matrix.deps }})${{ matrix.no-annotations == true && ' - Without Annotations' || '' }}${{ matrix.orm != '' && format(' - ORM {0}', matrix.orm) || '' }}"
    runs-on: "ubuntu-latest"

    services:
      mongo:
        image: mongo
        ports:
          - 27017:27017

    strategy:
      fail-fast: false
      matrix:
        php-version:
          - "7.4"
          - "8.0"
          - "8.1"
          - "8.2"
          - "8.3"
          - "8.4"
          - "8.5"
        deps:
          - "highest"
        no-annotations:
          - false
        orm:
          - ""
        include:
          - deps: "lowest"
            php-version: "7.4"
          - deps: "highest"
            php-version: "8.5"
          # Run builds on low and high PHP versions with `doctrine/annotations` removed
          - deps: "highest"
            php-version: "7.4"
            no-annotations: true
          - deps: "highest"
            php-version: "8.5"
            no-annotations: true
          # Run builds on high PHP version with `doctrine/orm` version pinned
          - deps: "highest"
            php-version: "8.5"
            orm: "^2.14"
          - deps: "highest"
            php-version: "8.5"
            orm: "^3.0"

    steps:
      - name: "Checkout"
        uses: "actions/checkout@v6"
        with:
          fetch-depth: 2

      - name: "Install PHP"
        uses: "shivammathur/setup-php@v2"
        with:
          php-version: "${{ matrix.php-version }}"
          extensions: mongodb
          coverage: "pcov"

      # Remove PHP-CS-Fixer to avoid conflicting dependency ranges (i.e. doctrine/annotations)
      - name: "Remove PHP-CS-Fixer"
        run: "composer remove --dev --no-update friendsofphp/php-cs-fixer"

      # Remove doctrine/annotations if configured to do so
      - name: "Remove doctrine/annotations"
        if: "${{ matrix.no-annotations }}"
        run: "composer remove --dev --no-update doctrine/annotations"

      # Pin doctrine/orm if configured to do so
      - name: "Pin doctrine/orm"
        if: "${{ matrix.orm }}"
        run: "composer require --dev --no-update doctrine/orm:${{ matrix.orm }}"

      - name: "Install dependencies with Composer"
        uses: "ramsey/composer-install@v3"
        with:
          dependency-versions: "${{ matrix.deps }}"

      - name: "Run PHPUnit"
        run: "vendor/bin/phpunit --exclude-group parse --coverage-clover coverage.xml"

      - name: "Upload coverage file"
        uses: "actions/upload-artifact@v6"
        with:
          name: "${{ github.job }}-${{ matrix.php-version }}-${{ matrix.deps }}-${{ matrix.no-annotations == true && 'no-annotations' || 'with-annotations' }}${{ matrix.orm != '' && format('-orm-{0}', matrix.orm) || '' }}-coverage"
          path: "coverage.xml"

  phpunit-parse:
    name: "PHPUnit Parse Adapter (PHP ${{ matrix.php-version }})"
    runs-on: "ubuntu-latest"

    services:
      mongo:
        image: mongo:5.0.17

      parse-server:
        image: zeliard91/parse-server:6.1.0
        env:
          PARSE_SERVER_DATABASE_URI: mongodb://mongo:27017/gedmo_parse_test
          PARSE_SERVER_APPLICATION_ID: u88dwhX625F5449gB64zc3AWNs64TUwMFVgRwgYkr
          PARSE_SERVER_MASTER_KEY: 7TH7Pc7p8m2zw4P7turJp3pJ467iVMzVkkTU9RX66
          PARSE_SERVER_REST_API_KEY: h8irZEmQ8yaXn7xESVS7nf5Yw87F3Q6c34E78sbU2
          PARSE_SERVER_ALLOW_CLIENT_CLASS_CREATION: 1
          PARSE_SERVER_MASTER_KEY_IPS: 0.0.0.0/0
        ports:
          - 1338:1337

    strategy:
      fail-fast: false
      matrix:
        php-version:
          - "8.1"
          - "8.2"
          - "8.3"
          - "8.4"

    steps:
      - name: "Checkout"
        uses: "actions/checkout@v6"
        with:
          fetch-depth: 2

      - name: "Install PHP"
        uses: "shivammathur/setup-php@v2"
        with:
          php-version: "${{ matrix.php-version }}"
          extensions: mongodb

      # Remove PHP-CS-Fixer to avoid conflicting dependency ranges (i.e. doctrine/annotations)
      - name: "Remove PHP-CS-Fixer"
        run: "composer remove --dev --no-update friendsofphp/php-cs-fixer"

      - name: "Install dependencies with Composer"
        uses: "ramsey/composer-install@v3"
        with:
          dependency-versions: "highest"

      - name: "Run PHPUnit (Parse Adapter suite)"
        run: 'vendor/bin/phpunit --testsuite="Parse Adapter"'

  lint-doctrine-xml-schema:
    name: Lint Doctrine XML schemas

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: "Install PHP"
        uses: "shivammathur/setup-php@v2"
        with:
          php-version: "8.2"
          extensions: mongodb

      - name: "Install dependencies with Composer"
        uses: "ramsey/composer-install@v3"
        with:
          dependency-versions: "highest"

      - name: Install required dependencies
        run: sudo apt-get update && sudo apt-get install libxml2-utils

      - name: Lint xml files
        run: make lint-doctrine-xml-schema

  upload_coverage:
    name: "Upload coverage to Codecov"
    runs-on: "ubuntu-latest"
    needs:
      - "phpunit"

    steps:
      - name: "Checkout"
        uses: "actions/checkout@v6"
        with:
          fetch-depth: 2

      - name: "Download coverage files"
        uses: "actions/download-artifact@v7"
        with:
          path: "reports"

      - name: "Upload to Codecov"
        uses: "codecov/codecov-action@v5"
        with:
          directory: reports
          token: "${{ secrets.CODECOV_TOKEN }}"
